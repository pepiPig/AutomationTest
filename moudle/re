import re

#match 从字符串的开头位置匹配，从开头可以匹配到，就获得一个正则对象
#匹配不到返回None

re.match("正则表达式","目标字符串")
re.match().group()

re.search("abc","e12312abc") #在目标字符串的任意位置，只会成功匹配一次就结束了，不会再尝试多次匹配

re.findall("abc","aasabcsd you abc")

#编译正则表达式
pattern=re.compile("abc")
pattern.match("abcda")

if re.match():
   print(re.match.group())
else:
   print("匹配失败")
